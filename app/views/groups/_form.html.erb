<%= form_for :group, :url => { :action => @submit, :id => @group.try(:id) } do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t("group.name") %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label t("group.number") %><br />
    <%= f.text_field :number %>
  </div>

  <div class="actions">
    <%= f.submit t("menu.#{@submit}"), :disable_with => t("menu.#{@submit}") %>
  </div>
<% end %>
